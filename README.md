# Proyecto Laravel - Logística

Este es un sistema desarrollado en Laravel para la gestión de logística.

## Instalación

```bash
composer install
npm install
cp .env.example .env
php artisan key:generate
php artisan migrate
